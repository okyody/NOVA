apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nova
  namespace: nova
spec:
  interval: 5m
  chart:
    spec:
      chart: ./deploy/helm/nova
      sourceRef:
        kind: GitRepository
        name: nova
